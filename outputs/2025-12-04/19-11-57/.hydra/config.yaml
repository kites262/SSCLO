dataset_name: HanChuan
work_dir: ${hydra:runtime.cwd}
data_dir: ${work_dir}/data
train_samples: 30
val_samples: 10
max_epochs: 200
learning_rate: 0.0003
split_image: true
swanlab_mode: offline
model:
  model_name: MambaHSI
  instance:
    _target_: models.MambaHSI.MambaHSI
